Local Docker Apache image for testing: (localhost:8080)
----
navigate to the resources folder in the terminal
docker run -p 8080:80 -v $(pwd):/usr/local/apache2/htdocs/ httpd:2.4

Clean up old docker stuff (like non-running containers)
------
docker system prune